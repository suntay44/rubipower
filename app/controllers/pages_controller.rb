class PagesController < ApplicationController
  def home
    # This will redirect to login since it's protected
  end

  def about
    # This will redirect to login since it's protected
  end

  def contact
    # This will redirect to login since it's protected
  end

  def profile
    # This will redirect to login since it's protected
  end

  def settings
    # This will redirect to login since it's protected
  end
end
