<!-- Main Content -->
    <!-- Top Navigation -->
    <header class="bg-white shadow-sm border-b border-gray-200">
      <div class="flex items-center justify-between px-6 py-4">
        <div class="flex items-center space-x-4">
          <a href="<%= cash_advance_request_path(@cash_advance_request) %>" class="text-gray-500 hover:text-gray-700">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
            </svg>
          </a>
          <div class="flex items-center space-x-3">
            <h1 class="text-2xl font-bold text-gray-900">Expense Report</h1>
          </div>
        </div>
        
        <div class="flex items-center space-x-4">
          <a href="<%= edit_expense_report_path(@expense_report) %>" class="bg-gray-600 text-white px-4 py-2 rounded-md hover:bg-gray-700 transition-colors duration-200 flex items-center space-x-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
            </svg>
            <span>Edit</span>
          </a>
        </div>
      </div>
    </header>

    <!-- Content -->
    <div class="p-6">
      <div class="max-w-6xl mx-auto">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <!-- Main Content -->
          <div class="lg:col-span-2 space-y-6">
            <!-- Cash Advance Request Info -->
            <div class="bg-white rounded-lg shadow-sm p-6">
              <h2 class="text-lg font-semibold text-gray-900 mb-4">Cash Advance Request Details</h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Employee Name</label>
                  <div class="text-sm text-gray-900"><%= @cash_advance_request.employee_name %></div>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Employee ID</label>
                  <div class="text-sm text-gray-900"><%= @cash_advance_request.employee_id %></div>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Department</label>
                  <div class="text-sm text-gray-900"><%= @cash_advance_request.department %></div>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Amount Requested</label>
                  <div class="text-sm text-gray-900">₱<%= number_with_precision(@cash_advance_request.amount_requested, precision: 2) %></div>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Purpose</label>
                  <div class="text-sm text-gray-900"><%= @cash_advance_request.purpose_of_advance %></div>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Required Date</label>
                  <div class="text-sm text-gray-900"><%= @cash_advance_request.required_date.strftime("%b %d, %Y") %></div>
                </div>
              </div>
            </div>

            <!-- Expense Report Details -->
            <div class="bg-white rounded-lg shadow-sm p-6">
              <h2 class="text-lg font-semibold text-gray-900 mb-4">Expense Report Details</h2>
              <div class="space-y-6">
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Explanation of Actual Expenses</label>
                  <div class="text-sm text-gray-900 whitespace-pre-line"><%= @expense_report.explanation %></div>
                </div>
                
                <div>
                  <label class="block text-sm font-medium text-gray-700 mb-2">Unused Cash Returned</label>
                  <div class="text-sm text-gray-900">₱<%= number_with_precision(@expense_report.unused_cash, precision: 2) %></div>
                </div>

                <!-- Receipts -->
                <% if @expense_report.receipts.attached? %>
                  <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Receipts</label>
                    <div class="space-y-2">
                      <% @expense_report.receipts.each do |receipt| %>
                        <div class="flex items-center justify-between p-2 bg-blue-50 rounded border">
                          <div class="flex items-center space-x-2">
                            <span class="text-blue-600">📎</span>
                            <span class="text-sm text-gray-700"><%= receipt.filename %></span>
                            <span class="text-xs text-gray-500">(<%= number_to_human_size(receipt.byte_size) %>)</span>
                          </div>
                          <div class="flex items-center space-x-2">
                            <%= link_to "Download", rails_blob_path(receipt, disposition: "attachment"), class: "text-xs text-blue-600 hover:text-blue-800" %>
                          </div>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>

          <!-- Sidebar -->
          <div class="space-y-6">
            <!-- Summary -->
            <div class="bg-white rounded-lg shadow-sm p-6">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Summary</h3>
              <div class="space-y-3">
                <div class="flex justify-between">
                  <span class="text-sm text-gray-600">Amount Requested:</span>
                  <span class="text-sm font-medium text-gray-900">₱<%= number_with_precision(@cash_advance_request.amount_requested, precision: 2) %></span>
                </div>
                <div class="flex justify-between">
                  <span class="text-sm text-gray-600">Unused Cash:</span>
                  <span class="text-sm font-medium text-gray-900">₱<%= number_with_precision(@expense_report.unused_cash, precision: 2) %></span>
                </div>
                <div class="border-t pt-3">
                  <div class="flex justify-between">
                    <span class="text-sm font-medium text-gray-900">Actual Expenses:</span>
                    <span class="text-sm font-medium text-gray-900">₱<%= number_with_precision(@cash_advance_request.amount_requested - @expense_report.unused_cash, precision: 2) %></span>
                  </div>
                </div>
              </div>
            </div>

            <!-- Submitted By -->
            <div class="bg-white rounded-lg shadow-sm p-6">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">Submitted By</h3>
              <div class="space-y-2">
                <div>
                  <span class="text-sm text-gray-600">Name:</span>
                  <span class="text-sm text-gray-900 ml-2"><%= @expense_report.submitted_by.first_name %> <%= @expense_report.submitted_by.last_name %></span>
                </div>
                <div>
                  <span class="text-sm text-gray-600">Date:</span>
                  <span class="text-sm text-gray-900 ml-2"><%= @expense_report.created_at.strftime("%b %d, %Y at %I:%M %p") %></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Sidebar Script -->
    <%= render 'shared/sidebar_script' %>

